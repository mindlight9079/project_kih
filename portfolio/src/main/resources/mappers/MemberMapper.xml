<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.green.portfolio.dao.MemberDAO">
	<select id="getMember" resultType="kr.green.portfolio.vo.MemberVO">
		select * from member where id = #{id}
	</select>
	<insert id="signup">
		insert into member(me_id, me_nickname, me_password, me_gender, me_birth, me_phone, me_address, me_name, me_resident_num, me_email, me_point, me_grade) 
			values(#{me_id}, #{me_nickname}, #{me_password}, #{me_gender}, #{me_birth}, #{me_phone}, #{me_address}, #{me_name}, #{me_resident_num}, #{me_email}, #{me_point}, #{me_grade})
	</insert>
</mapper>

